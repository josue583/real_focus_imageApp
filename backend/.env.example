# Port for the server (optional, default 5000)
PORT=5000

# MongoDB connection string (optional). If set, app uses MongoDB for packages, bookings, gallery, and admin password.
# If not set, data is stored in backend/data/*.json files.
# Example: mongodb://localhost:27017/real-focus-image   or your Atlas URI
# MONGODB_URI=mongodb://localhost:27017/real-focus-image

# Admin password – the app reads from .env, NOT from this file!
# 1. Create .env: copy this file and name it .env   (e.g. copy .env.example .env)
# 2. Edit the .env file (not this one!) and set ADMIN_PASSWORD=YourPassword
# 3. Restart the backend. Log in at /admin with that password.
ADMIN_PASSWORD=your-secret-password
